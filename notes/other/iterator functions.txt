iterator tools (from itertools)
	product(list1, list2, repeat = x) # gives cartesian product of two lists
	permutations(list) #gives all permutations
	permutations(list, n)
	combination(list, n) #gives all possible combinations, no repetition
	combination_with_replacement(list, n)
	accumulate(list) #gives cumulated list 
	accumulate(list, operator) #operator imported from operators
	groupby(list, lambda) #groups list by key

	count(x) #iterates infinitely from x
	cycle(list) #cycles infinitely through list values
	repeat(x, n) # repeats x, n times

log handling functions (from logging)
	debug()
	info()
	warning()
	error()
	critical()
	BasicConfig(level, format, dateformat)
	logger = getLogger(__name__) #where logger is obj
	StreamHandler()
	FileHandler("file")
	setLevel()
