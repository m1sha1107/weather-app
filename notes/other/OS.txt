from os

getcwd() #returns current working directory
chdir('dir') #changes working directory
listdir('dir') #lists folders in dir. default is cwd

mkdir('name')
makedirs('name') #creates directory. allows sub dirs.
 
rmdir('dir') #deletes dir
removedirs('dirs') # #deletes dir with intermediaries

rename('originalname', 'newname')
stat('dir')
walk('dir') #yields dirtree

from os.path
path.join('dir1', 'dir2') #joins dirs for valid directory
path.basename('dir') #gives filename
path.dirname('dir')
path.split('dir')
path.exists('dir') #returns boolean
path.isdir('dir')
path.isfile('dir')
path.splitext('dir') #splits extensions

variables
environ #list of environment variables
os.environ.get("VAR") #gets home variable 

from datetime.datetime
fromtimestamp(stat('dir').st_mtime) for last modified date