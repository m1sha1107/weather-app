#from tkinter
Tk()
Toplevel()
wind.mainloop()
wind.quit()
wind.destroy() #used for toplevel

root.title("")
root.iconbitmap(r"directory")
root.geometry("Xxy")

widge.pack()
	sticky
	padx
	pady

widge.grid(row=, column=)
	sticky

widge.grid_forget()

Label(container, text="")
	anchor
	relief

	text
	image 
	relief (sink)
	bd (border)


Button(container,text="",command=,state=, padx=, pady=, fg=, bg=) #root and text mandatory
	command
	padx
	pady
	state (NORMAL, DISABLED)
	
	fg
	bg
	activebackground
	font
	image
	justify (LEFT, CENTER, RIGHT)

Entry(container, width=, borderwidth, fg, bg) #root is mandatory
	width
	borderwidth
	command
	columnspan
	
	fg
	bg
	cursor 
	font
# taking e as entry object:
e.get()
e.insert(start, "str")
e.delete(start, end)


LabelFrame(container)
	text
	padx
	pady



Radiobutton(container)
	text
	variable
	value
# all variables must be taken as either Intvar() or StringVar() not int() or str()
#taking r as tkinter variable:
r.get()
r.set()

from messagebox
messagebox.showinfo("title", "message")
messagebox.showwarning("title", "message")
messagebox.showerror("title", "message")
messagebox.askquestion("title", "message") #returns string
messagebox.askyesno("title", "message") #returns int
messagebox.askokcancel("title", "message")


Toplevel()

#from PIL.ImageTk
Photoimage.(im_obj)
#from Image
open(r"dir")

#from tkinter.filedialog
askopenfilename()
	title
	initialdir
	filetypes

Scale(container) #slider
	from_
	to
	orient (HORIZONTAL, VERTICAL)
s.set()

CheckButton(cont)
	text
	variable
	command
	onvalue
	offvalue
c.select()
c.deselect()

OptionMenu(cont, var, "option1", option2")
	*options (lists)
#*options is list
#many options can be created
#var must be StringVar

