from numpy as np 

np.array(list, *dtype)
np.zeros(n) #create array withzeroes. n mustbe int or list 
np.ones(n)
np.full(n, val)
x.numpy() # creates np array from torch tensor
np.identity(size)

~np from files~
np.genfromtxt('dir', *delimeter)

np.any(condition, *axis)
np.all(condition, *axis)

~operations~
np.repeat(arr, times, *axis) #axis is outermost axis
a.copy() #copies array
np.delete(n)
a.astype('type')

~math~
np.sin(a) 
np.matmul(a, b) #returns matrix product
np.min(a, *axis)
np.max(a, *axis)
np.sum(a, *axis)

									~linear algebra~
np.dot(np.dot)
np.matmul(a, b)
np.linalg.det(a)
np.linalg.norm(x, *axis)

~randoms~
np.random.rand(x, y) # creates array of rand floats
np.random.random_sample(n)
np.random.randint(lowerlim, upperlim, *size)

~reorganizing~
a.reshape(n)
np.appen(a, b, *axis)
np.vstack([a, b])
np.hstack([[a, b])

~ARRAY PROPERTIES~
a.ndim #get no. of dimensions
a.shape #get dimensions
a.dtype #get dtype
a.itemsize #get size of elements in bytes
a.size #get number of elements
a.nbytes #get total size in bytes