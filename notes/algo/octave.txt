PS1('prompt') %changes prompt
help %shows documentation
format TYPE %formats variable to TYPE
who
whos

pwd %working dir
cd 'dir'
ls
load
clear
save
addpath('path')

disp() %print
sprintf(str1, str2) %appends str2 to str1 at placeholder '%'

ones(r ,c)
zeros(r, c)
rand(r, c)
randn(r, c)
eye(n) %identity matrix
magic(n)
size(m)
size(m, 1)
size(m, 2)
length(v)
find(m)
pinv(m)

flipud(m)

sqrt(n)
log(n)
exp(n)
abs(n)
max(n)
min(n)
floor(a)
ceil(a)

sum(a)
prod(a)
std(a) %standard deviation
avg(a)

plot(x, y)
hold on
xlabel('lab')
ylabel('lab')
plot(m, 'rx', 'markersize, msize')

legend('plot1leg', 'plot2leg')
title('title')
print -dpng 'title.png'
close

figure(n)
subplot(x, y, n) %divides figure into XxY plot, and access the nth element
axis([x1 x2 y1 y2])

hist(matrix, bins)