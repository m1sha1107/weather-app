from sqlite3

connect("name.db") #creates/connects to db in memory
connect(":memory:") #creates/connects to db in memory
conn.close() #closes db

conn.cursor() #creates cursor
c.execute("""sql""") #executes sql 
c.executemany("""sql""", list) 
conn.commit() #commits changes


c.fetchone() #fetches first row
c.fetchmany(n) # fetches first n rows
c.fetchall() #fetches all selected rows

~SQL~

CREATE TABLE tablename ( var1 DATATYPE, var2 DATATYPE)
DROP TABLE tablename

INSERT INTO tablename VALUES (val1, val2) 
#ins. one record

INSERT INTO tablename VALUES (?,?) 
#ins. many records
# ? is placeholder

SELECT * FROM tablename
# select rows

SELECT * FROM tablename WHERE var1 = n
#selects columns where var1 = n

UPDATE tablename SET var1 = n WHERE var2 = y
#updates database

DELETE * from tablename WHERE var2 = y

SELECT * FROM tablename ORDER BY var1 ASC
#can be ordered by ASC DESC